#include "main.h"
                    	
/**
 * _isdigit - program checks for a digit
 * @c: value to be checked
 * Return: 1 if it's a digit else 0.
 */
int _isdigit(int c)
{
        	return (c >= '0' && c <= '9');
}
